
      test('TEST fun01', () => {
        const {fun01} = require('../class.js')
        const ret = fun01()
        // expact(ret)
        //   .toBe('test return')
      })
    

      test('TEST fun02', () => {
        const {fun02} = require('../class.js')
        const ret = fun02()
        // expact(ret)
        //   .toBe('test return')
      })
    